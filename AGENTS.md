# AGENTS.md

# 데스크톱 컴패니언 프로젝트

## 프로젝트 개요

이 프로젝트는 Swift로 작성된 네이티브 macOS 데스크톱 컴패니언이다.

애플리케이션은 로컬 AI 코딩 어시스턴트의 상태를 시각적으로 보여주는 픽셀 아트
캐릭터를 데스크톱에 표시한다.

캐릭터 자체가 AI인 것은 아니다.

캐릭터의 역할은 다음과 같다.

- AI 활동 시각화
- 가벼운 데스크톱 상호작용 제공
- 워크스페이스 실행 및 열기
- 현재 작업 상태 표시
- 데스크톱 컴패니언 역할

AI가 코딩을 수행하고 캐릭터는 그 활동을 시각화한다.

---

# 현재 목표(MVP)

현재 목표:

Codex CLI와 함께 작동하는 안정적인 macOS 데스크톱 컴패니언을 만든다.

MVP에 포함되는 기능:

- 투명 배경의 데스크톱 캐릭터
- 캐릭터 이동
- 캐릭터 애니메이션
- 캐릭터 상태 머신
- 로그인 시 자동 실행
- 잠자기 및 깨우기 복구
- Codex CLI 연동
- 현재 워크스페이스 열기
- 간단한 설정 창

MVP가 안정화될 때까지 향후 기능을 구현하지 않는다.

---

# 아키텍처

사용자

↓

데스크톱 컴패니언

↓

AI 제공자

↓

Codex CLI

↓

프로젝트 워크스페이스

컴패니언은 프로젝트 파일을 직접 수정하지 않는다.

컴패니언은 AI 제공자와 통신한다.

---

# 중요한 아키텍처 규칙

다음 계층은 항상 서로 분리한다.

캐릭터 계층

- 렌더링
- 이동
- 애니메이션
- 말풍선

상태 계층

- `CharacterState`
- `AppState`

AI 계층

- `AIProvider` 프로토콜
- `AIEvent`
- 제공자 구현체

워크스페이스 계층

- VS Code
- 프로젝트 경로
- 워크스페이스 상태

생명주기 계층

- 시작
- 로그인 항목
- 잠자기
- 깨우기
- 복원

보안 계층

- 권한
- 안전한 저장소

이 계층들의 책임을 절대 뒤섞지 않는다.

---

# AI 제공자

이 애플리케이션은 API 우선 방식으로 동작하지 않는다.

로컬에 설치된 AI 도구와 통신한다.

현재 제공자:

- Codex CLI

향후 추가할 수 있는 제공자:

- Claude Code
- 기타 로컬 코딩 어시스턴트

애플리케이션은 반드시 `AIProvider`를 통해서만 통신해야 한다.

애플리케이션 로직이 Codex에 직접 결합되지 않도록 한다.

Codex 전용 파싱 로직은 `CodexCLIProvider` 안에만 둔다.

---

# 캐릭터 상태

캐릭터 애니메이션은 오직 `CharacterState`에 의해 구동된다.

Codex 이벤트에서 애니메이션을 직접 실행하지 않는다.

흐름:

AI 이벤트

↓

`CharacterState`

↓

`AnimationController`

현재 상태:

- `idle`
- `thinking`
- `working`
- `waitingApproval`
- `success`
- `error`
- `disconnected`

`idle`은 AI 작업이 없을 때 사용하는 기본 자동 상태다.

`idle` 상태의 캐릭터는 자유롭게 돌아다니며 걷는 모습의 애니메이션을
사용한다.

사용자 요청으로 AI 작업이 시작되면 `working` 상태로 전환하고, 작업이
끝나면 결과 상태를 표시한 뒤 `idle`로 돌아간다.

`working`은 코드 작성, 파일 처리, 명령 실행 등 AI가 작업을 수행하는 상태를
통합해서 나타낸다.

`working` 상태는 코딩하는 모습을 표현하는 애니메이션을 사용한다.

---

# 개발 전략

점진적으로 개발한다.

한 번에 하나의 기능만 구현한다.

각 기능을 완료할 때 다음 조건을 충족해야 한다.

1. 컴파일된다.
2. 실행된다.
3. 수동 테스트를 완료한다.
4. 커밋한다.

여러 개의 대규모 시스템을 동시에 구현하지 않는다.

---

# 현재 개발 순서

1. 투명한 캐릭터 창
2. 드래그
3. 캐릭터 이동
4. `CharacterState`
5. 애니메이션 시스템
6. 로그인 항목
7. 잠자기 및 깨우기 처리
8. `AIProvider` 추상화
9. `CodexCLIProvider`
10. 워크스페이스 연동
11. 설정
12. 스케줄러

---

# 보안

컴패니언은 제한 없는 접근 권한이 있다고 가정해서는 안 된다.

규칙:

- 승인받은 프로젝트 폴더에만 접근한다.
- 위험한 작업에는 확인이 필요하다.
- 임의의 사용자 파일에 접근하지 않는다.
- 컴패니언과 Codex의 책임을 분리한다.

---

# 성능

단순함을 우선한다.

다음을 피한다.

- 불필요한 추상화
- 이른 최적화
- 과도한 설계

데스크톱 컴패니언은 유휴 상태에서 CPU와 메모리를 최소한으로 사용해야 한다.

---

# 향후 확장

아키텍처는 향후 다음 기능을 지원할 수 있어야 한다.

- 추가 로컬 AI 제공자
- Windows 버전
- 여러 캐릭터
- 오피스 UI
- 스케줄러 개선

단, MVP가 완성되고 안정화될 때까지 향후 확장 기능을 구현하지 않는다.

---

# 코딩 원칙

- 각 파일이 하나의 명확한 역할에 집중하도록 한다.
- 상속보다 조합을 선호한다.
- 전역 상태를 피한다.
- 설정 화면에는 SwiftUI를 사용한다.
- 데스크톱 창 관리에는 AppKit을 사용한다.
- 비즈니스 로직을 UI와 독립적으로 유지한다.
- 영리하지만 이해하기 어려운 코드보다 읽기 쉬운 코드를 선호한다.

판단이 확실하지 않을 때는 책임을 분리할 수 있는 가장 단순한 아키텍처를 선택한다.
